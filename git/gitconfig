[core]
    editor = nvim
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    line-numbers = true
    side-by-side = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[alias]
	clone-bare-worktree = "!sh $HOME/.git/git-clone-bare-for-worktrees.sh"

[include]
    path = ~/.gitconfig_local
